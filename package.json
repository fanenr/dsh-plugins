{
  "name": "dsh-plugins",
  "description": "DSH Plugins",
  "type": "module",
  "version": "0.1.1",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  }
}
